# 统一换行符，避免 Windows / macOS 之间来回提交时产生整文件 diff
* text=auto eol=lf

# 测试数据要保持原样：里面的换行和制表符是判题依据的一部分，
# 不能让 git 做任何转换（评测脚本自己会处理换行差异）
tests/cases/** -text

# 二进制文件
*.png binary
*.jpg binary
*.pdf binary
*.docx binary
