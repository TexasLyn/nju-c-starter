/*
 * L03_2 · math.h 函数练习
 *
 * 【题目】（经典题）
 *   读入两个数 x 和 y，计算并输出下面五个结果：
 *
 *       sin(x)      x 的正弦
 *       cos(x)      x 的余弦
 *       |x|         x 的绝对值
 *       e^x         e 的 x 次方
 *       x^y         x 的 y 次方
 *
 * 【输入格式】
 *   两个数，用空格分隔，例如：
 *   1 2
 *
 * 【输出格式】
 *   五行，等号两边没有空格，每个结果保留 6 位小数：
 *
 *   sin=0.017452
 *   cos=0.999848
 *   abs=1.000000
 *   exp=2.718282
 *   pow=1.000000
 *
 * 【⚠️ 最重要的提示：x 是角度，要先转成弧度】
 *   C 的 sin/cos 接受的单位是**弧度**，不是度。
 *   直接把 1 传进去，算的是 sin(1 弧度) = 0.841471，完全不对。
 *
 *   转换公式：弧度 = 角度 × π ÷ 180
 *
 *   而 π 在 math.h 里**没有定义**，你得自己写一个：
 *       #define PI 3.14159265358979323846
 *   这一行放在 #include 的下面。
 *
 * 【其他提示】
 *   - 绝对值用 fabs（不是 abs，abs 是给整数用的）
 *   - e^x 用 exp(x)
 *   - x^y 用 pow(x, y)
 *   - 千万别写成 x^y，^ 在 C 里不是乘方，是按位异或（第 8 课会提）
 *
 * 【验算方法】
 *   如果算出来 sin 是 0.841471 而不是 0.017452，说明你忘了转弧度。
 */

#include <stdio.h>
#include <math.h>

int main(void)
{
    double x, y;

    scanf("%lf %lf", &x, &y);

    // TODO 1: 定义 PI
    // #define PI ...

    // TODO 2: 把角度 x 转成弧度（存到另一个变量里，别把 x 本身改掉，
    //         因为后面 e^x 和 x^y 还要用原来的 x）

    // TODO 3: 输出五行结果

    return 0;
}
