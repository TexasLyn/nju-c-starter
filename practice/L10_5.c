/*
 * L10_5 · 找错题：数组越界
 *
 * 【题目】
 *   下面这段程序想读入 5 个数并求和。它能编译通过，但有一个错误，
 *   这个错误会导致最后的输出不对（或者程序崩溃）。
 *
 *   请找出错误并写出正确的程序。
 *
 * 【有错误的程序】
 *   ────────────────────────────────────────
 *   #include <stdio.h>
 *
 *   int main(void)
 *   {
 *       int a[5];
 *       int sum = 0;
 *
 *       for (int i = 0; i <= 5; i++) {
 *           scanf("%d", &a[i]);
 *       }
 *
 *       for (int i = 0; i <= 5; i++) {
 *           sum = sum + a[i];
 *       }
 *
 *       printf("%d\n", sum);
 *       return 0;
 *   }
 *   ────────────────────────────────────────
 *
 * 【错误分析】
 *
 *   两处循环的条件都是 `i <= 5`，但数组 `a` 只有 5 个元素，
 *   合法下标是 0、1、2、3、4。i 等于 5 时就【越界】了。
 *
 *   ⚠️ 【关键：这个错误不会报错】
 *       第一个循环里 `scanf("%d", &a[5])` 会读入一个数并写进
 *       a 后面那块内存 —— 那块内存可能属于别的变量（比如 sum！）。
 *       于是 sum 被莫名其妙地改掉，最后的答案就错了。
 *
 *       第二个循环里读 `a[5]` 会得到一个垃圾值，加到 sum 上。
 *
 *   两个错误叠加，结果完全不可预测。而且【时对时错】——
 *   取决于编译器把 sum 放在内存的什么位置。
 *
 * 【正确写法】
 *   循环条件改成 `i < 5`。
 *
 * 【⚠️ 怎么避免这个错误】
 *   记住口诀：数组有 n 个元素时，下标范围是 0 到 n-1，
 *   所以循环条件写 `i < n`，【永远不要写 i <= n】。
 *
 *   另一种防错方法：把 n 定义成宏，然后用 n 写循环，
 *   这样即使以后改了数组大小，循环也跟着改：
 *
 *       #define N 5
 *       int a[N];
 *       for (int i = 0; i < N; i++) { ... }
 *
 * 【你要做的】
 *   写出正确的程序，读入 5 个整数，输出它们的和。
 *
 * 【测试数据】
 *   输入 1 2 3 4 5    →  输出 15
 *   输入 -1 1 -1 1 0  →  输出 0
 */

#include <stdio.h>

#define N 5

int main(void)
{
    int a[N];
    int sum = 0;

    // TODO: 正确的输入循环和求和循环

    printf("%d\n", sum);

    return 0;
}
