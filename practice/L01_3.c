/*
 * L01_3 · 找错题（上机题原题型！）
 *
 * 【题目】
 *   下面这段程序想求两个整数的和，但它有 3 处错误，导致编译不通过。
 *   请把改正后的完整程序写在下面的 main 函数里。
 *
 * 【有错误的程序】
 *   ────────────────────────────────────────
 *   #include <stdio.h>
 *
 *   int main(void)
 *   {
 *       int a, b, sum
 *       a = 10;
 *       b = 20
 *       sum = a + b;
 *       printf("和是 %d\n", sum)
 *       return 0;
 *   }
 *   ────────────────────────────────────────
 *
 * 【提示】
 *   错误都是很基础的：分别看看
 *     - 每句话的末尾
 *     - 变量声明那一行
 *     - 有没有用到中文标点
 *
 * 【注意】
 *   这类题在上机卷里占 20 分（第一大题），
 *   规则是：只能改、不能增加或删除语句。所以请只改必要的字符。
 *
 * 【期望输出】
 *   和是 30
 */

#include <stdio.h>

int main(void)
{
    // TODO: 把改正后的程序写在下面这个 main 里
    // （上面那段有错的代码是让你看的，不要直接复制过来）
    int a,b,sum;
    a = 10;
    b = 20;
    sum = a + b;

    printf("和是 %d\n",sum);

    return 0;
}
