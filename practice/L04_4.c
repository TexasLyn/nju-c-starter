/*
 * L04_4 · 输入星期首字母，判断星期几
 *
 * 【题目】（经典题）
 *   读入表示星期几的英文首字母，判断并输出是星期几。
 *
 *   问题在于：M、W、F 是唯一的，但 T 和 S 各有两天：
 *       M → Monday
 *       T → 还要看第二个字母：Tu → Tuesday，Th → Thursday
 *       W → Wednesday
 *       F → Friday
 *       S → 还要看第二个字母：Sa → Saturday，Su → Sunday
 *
 * 【输入格式】
 *   分两种情况：
 *     - 如果首字母是 M / W / F，只输入 1 个字母：   M
 *     - 如果首字母是 T / S，输入 2 个字母：         Tu
 *   （每次输入以回车结束）
 *
 * 【输出格式】
 *   英文星期名，首字母大写，占一行：Monday
 *
 * 【提示】
 *   scanf(" %c", &c) 注意 %c 前面那个空格，它会跳过所有空白字符
 *   （包括回车）。这样第二次读字符时就不会读到上一行的回车了。
 *   这是第 3 课讲过的坑。
 *
 * 【结构建议】
 *   外层 switch 判断首字母，在 case 'T' 和 case 'S' 里面再读一个字符判断。
 *
 *   也可以全用 if 写，看你觉得哪种清楚。
 *
 * 【为什么要练这道题】
 *   它同时考了：switch 用法、读字符的坑、嵌套判断。
 *   上机题里这种"看起来简单但细节多"的题得分率很低，值得练熟。
 */

#include <stdio.h>

int main(void)
{
    char c1, c2;

    scanf(" %c", &c1);

    // TODO: 判断 c1 是哪个字母，输出对应的星期名
    //       如果是 T 或 S，先 scanf(" %c", &c2) 读第二个字母再判断

    return 0;
}
