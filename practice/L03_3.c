/*
 * L03_3 · 海伦公式求三角形面积
 *
 * 【题目】（经典题）
 *   读入三角形的三条边长，用海伦公式计算面积，保留 6 位小数输出。
 *
 * 【海伦公式】
 *   设 p = (a + b + c) / 2      （半周长）
 *   面积 = √(p × (p-a) × (p-b) × (p-c))
 *
 * 【输入格式】
 *   三个数，用空格分隔。
 *
 * 【输出格式】
 *   一个数，保留 6 位小数，占一行。
 *
 * 【样例】
 *   输入：2 3 2
 *   输出：1.984313
 *
 * 【提示】
 *   - 开平方用 sqrt(x)
 *   - 半周长 p 要用 double，别用 int！
 *         如果写成 int p = (a+b+c)/2，遇到 2 3 2 这种情况
 *         (2+3+2)/2 = 3.5 会被砍成 3，结果就全错了。
 *         所以：double p = (a + b + c) / 2.0;
 *   - 注意那个 2.0，写成 2 的话右边三个数都是 double 所以没关系，
 *     但养成写 2.0 的习惯更保险
 *
 * 【验算】
 *   p = (2+3+2)/2 = 3.5
 *   面积 = √(3.5 × 1.5 × 0.5 × 1.5) = √3.9375 = 1.984313...
 */

#include <stdio.h>
#include <math.h>

int main(void)
{
    double a, b, c;

    scanf("%lf %lf %lf", &a, &b, &c);

    // TODO 1: 算半周长 p

    // TODO 2: 用海伦公式算面积

    // TODO 3: 输出面积，保留 6 位小数，末尾换行

    return 0;
}
