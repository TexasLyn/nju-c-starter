/*
 * L04_5 · 找错题：能编译但结果全错
 *
 * 【题目】
 *   下面这段程序想判断 x 是不是"1 到 100 之间的正数"，
 *   是则输出 yes，否则输出 no。
 *
 *   它【能编译通过】（只有一个警告），但结果是错的：
 *   不管你输入什么，它都输出 yes。
 *
 *   请找出错误并写出正确的程序。
 *
 * 【有错误的程序】
 *   ────────────────────────────────────────
 *   #include <stdio.h>
 *
 *   int main(void)
 *   {
 *       int x;
 *       scanf("%d", &x);
 *
 *       if (0 < x < 100)
 *           printf("yes\n");
 *       else
 *           printf("no\n");
 *
 *       return 0;
 *   }
 *   ────────────────────────────────────────
 *
 * 【为什么全输出 yes】
 *   这是本课第 3 节讲过的坑：C 不支持连续比较。
 *   0 < x < 100 被理解成 (0 < x) < 100：
 *
 *       输入 -5：  0 < -5 得 0，  再算 0 < 100 得 1  → 真 → yes（错！）
 *       输入 50：  0 < 50 得 1，  再算 1 < 100 得 1  → 真 → yes（对）
 *       输入 200： 0 < 200 得 1， 再算 1 < 100 得 1  → 真 → yes（错！）
 *
 *   因为比较的结果只有 0 和 1，而 0 和 1 都小于 100，
 *   所以第二个比较永远成立。整个条件恒为真。
 *
 * 【正确写法】
 *   要表达"x 大于 0 并且 x 小于 100"，得用 && 分开写：
 *       if (0 < x && x < 100)
 *
 * 【你要做的】
 *   写出正确的程序，使输出符合要求：
 *       输入 50   →  yes
 *       输入 1    →  yes
 *       输入 100  →  no   （注意：100 不算，题目是"1 到 100 之间"）
 *       输入 0    →  no
 *       输入 -5   →  no
 *       输入 200  →  no
 */

#include <stdio.h>

int main(void)
{
    int x;

    scanf("%d", &x);

    // TODO: 写出正确的判断

    return 0;
}
