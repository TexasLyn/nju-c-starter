/*
 * L06_3 · 按规律生成前 20 项
 *
 * 【题目】（经典题）
 *   数列的前几项规则如下：
 *
 *       第 1 项  a1 = 1
 *       当 n 是偶数时：  a_n = a_{n-1} × 2
 *       当 n 是奇数时（n >= 3）：  a_n = a_{n-1} + n
 *
 *   输出这个数列的前 20 项，每行一个。
 *
 * 【期望输出】
 *   1
 *   2
 *   5
 *   10
 *   15
 *   30
 *   ...（共 20 行，最后一行是 6098）
 *
 * 【逐项验算（前几项）】
 *       n=1:  a1 = 1                        （已知）
 *       n=2:  n 是偶数 → a2 = a1 × 2 = 2
 *       n=3:  n 是奇数 → a3 = a2 + 3 = 5    ← 注意加的是 n 本身，不是固定值
 *       n=4:  n 是偶数 → a4 = a3 × 2 = 10
 *       n=5:  n 是奇数 → a5 = a4 + 5 = 15
 *       n=6:  n 是偶数 → a6 = a5 × 2 = 30
 *       n=7:  n 是奇数 → a7 = a6 + 7 = 37
 *
 * 【思路：一个循环 + 一个 if】
 *
 *       long a = 1;
 *       printf("%ld\n", a);              (先输出第 1 项)
 *
 *       for (int n = 2; n <= 20; n++) {
 *           if (n % 2 == 0) {
 *               a = a * 2;               (偶数项)
 *           } else {
 *               a = a + n;               (奇数项，加的是 n)
 *           }
 *           printf("%ld\n", a);
 *       }
 *
 * 【⚠️ 为什么用 long 而不是 int】
 *   第 20 项是 6098，其实 int 完全够。但因为中间有反复乘 2，
 *   养成用 long 的习惯更保险。long 的格式符是 %ld。
 *
 * 【⚠️ 最容易写错的地方】
 *   奇数项的规则是"加上 n"，其中 n 是【当前项号】，
 *   不是加固定值，也不是加 n-1。看清楚题目。
 */

#include <stdio.h>

int main(void)
{
    long a = 1;

    // TODO: 先输出第 1 项，然后循环生成第 2 到 20 项

    return 0;
}
