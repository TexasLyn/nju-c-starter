/*
 * L05_5 · 找错题：程序卡死 + 求和结果不对
 *
 * 【题目】
 *   下面这段程序想计算 1 + 2 + 3 + ... + 100 的和（正确答案是 5050）。
 *   但它有【两处】错误：程序会卡死不动。
 *
 *   请找出错误并写出正确的程序。
 *
 * 【有错误的程序】
 *   ────────────────────────────────────────
 *   #include <stdio.h>
 *
 *   int main(void)
 *   {
 *       int i = 1;
 *       int sum = 0;
 *
 *       while (i < 100) {
 *           sum = sum + i;
 *       }
 *
 *       printf("%d\n", sum);
 *       return 0;
 *   }
 *   ────────────────────────────────────────
 *
 * 【错误分析】
 *   错误 1（导致卡死）：循环体里【忘了让 i 变化】，i 永远是 1，
 *                       条件 i < 100 永远成立 → 死循环。
 *                       必须加一句 i++;
 *
 *   错误 2（边界差 1）：条件写的是 i < 100，那 i 最大只会到 99，
 *                       加不到 100，结果少 100。
 *                       应该是 i <= 100。
 *
 *   （这两处错误很典型：【一处让程序不动，一处让答案不对】。
 *     编程练习的改错题就是这种风格。）
 *
 * 【你要做的】
 *   写出正确的程序，使：
 *       输出 5050
 *
 * 【额外要求】
 *   改完之后，顺手想想：如果把 i++ 写在 sum = sum + i 的【前面】，
 *   结果会怎样？自己改一下跑跑看。
 *   （答案：会变成加 2 到 101，和是 5150，因为第一轮加的是已经变成 2 的 i。）
 *   这就是第 5 节说的"步进的位置很重要"。
 */

#include <stdio.h>

int main(void)
{
    // TODO: 写出正确的程序

    return 0;
}
