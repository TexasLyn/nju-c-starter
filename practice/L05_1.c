/*
 * L05_1 · 四元不定方程的整数解个数（for 和 while 各写一遍）
 *
 * 【题目】（经典题）
 *   求方程 x1 + x2 + x3 + x4 = 30 的【正整数】解有多少组。
 *   要求分别用 for 和 while 写一遍，两个结果都输出。
 *
 * 【输出格式】
 *   两个整数，用空格分隔，末尾换行：
 *   3654 3654
 *
 * 【为什么输出两个】
 *   和上一课的奖金题一样，这是一种自我检验：
 *   两种写法结果一致，说明你都写对了。
 *
 * 【思路】
 *   四层穷举。每一层的变量范围都是 1 到 27。
 *
 * 【⚠️ 为什么是 27 而不是 30】
 *   因为 x1、x2、x3、x4 都是【正整数】（最小是 1）。
 *   如果 x1 = 28，那剩下的 x2+x3+x4 = 2，三个正整数最小是 3，装不下。
 *   所以任何一个变量最大只能到 30 - 1 - 1 - 1 = 27。
 *
 *   范围写 30 也对，只是多算了一些不可能成立的组合，结果一样。
 *   但范围写小了（比如写 10）就会漏解。
 *   🔸 记个原则：穷举的范围宁可写大，不能写小。
 *
 * 【for 版本】
 *       int count1 = 0;
 *       for (int x1 = 1; x1 <= 27; x1++) {
 *           for (int x2 = 1; x2 <= 27; x2++) {
 *               for (int x3 = 1; x3 <= 27; x3++) {
 *                   for (int x4 = 1; x4 <= 27; x4++) {
 *                       if (x1 + x2 + x3 + x4 == 30) count1++;
 *                   }
 *               }
 *           }
 *       }
 *
 * 【while 版本】
 *   while 没有"步进"那一格，所以【循环体里必须自己写 x++】。
 *   这是最容易忘的地方，忘了就是死循环。
 *
 *       int count2 = 0, x1 = 1;
 *       while (x1 <= 27) {
 *           int x2 = 1;                    // 内层变量必须在这里重新初始化！
 *           while (x2 <= 27) {
 *               ...
 *               x2++;                      // 别忘了
 *           }
 *           x1++;                          // 别忘了
 *       }
 *
 * 【⚠️ while 版最容易犯的错】
 *   内层变量 x2、x3、x4 必须在【外层循环体内部】初始化。
 *   如果写在最外面（int x2 = 1; 只写一次），那么第一轮结束后 x2 就是 28，
 *   第二轮开始 x2 不会回到 1，内层循环一次都不执行 → 结果少一大堆。
 */

#include <stdio.h>

int main(void)
{
    int count1 = 0;    /* for 版本的结果 */
    int count2 = 0;    /* while 版本的结果 */

    // TODO 1: 用四层 for 算出 count1

    // TODO 2: 用四层 while 算出 count2

    printf("%d %d\n", count1, count2);

    return 0;
}
