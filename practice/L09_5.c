/*
 * L09_5 · #ifdef 和 #if 的阅读题
 *
 * 【题目】（经典题）
 *   请【先在纸上算】下面程序会输出哪几行，再写代码验证。
 *
 * 【要分析的代码】
 *   ────────────────────────────────────────
 *   #include <stdio.h>
 *
 *   #define Z 0
 *   #define Y 1
 *
 *   int main(void)
 *   {
 *   #ifdef Z
 *       printf("A\n");
 *   #endif
 *
 *   #ifndef Z
 *       printf("B\n");
 *   #endif
 *
 *   #if Z
 *       printf("C\n");
 *   #endif
 *
 *   #ifdef Y
 *       printf("D\n");
 *   #endif
 *
 *   #if Y
 *       printf("E\n");
 *   #endif
 *
 *       return 0;
 *   }
 *   ────────────────────────────────────────
 *
 * 【逐条分析（先自己算，再往下看）】
 *
 *   `#define Z 0`  →  Z 被定义过，值是 0
 *   `#define Y 1`  →  Y 被定义过，值是 1
 *
 *   #ifdef Z    Z 定义过吗？  → 定义过  → 成立  → 编译并输出 A
 *   #ifndef Z   Z 没定义过吗？→ 定义过  → 不成立 → 不编译 B
 *   #if Z       Z 的值是 0 吗？→ 0 是假 → 不成立 → 不编译 C
 *   #ifdef Y    Y 定义过吗？  → 定义过  → 成立  → 编译并输出 D
 *   #if Y       Y 的值是 1 吗？→ 1 是真 → 成立  → 编译并输出 E
 *
 *   输出三行：A、D、E
 *
 * 【本题的核心考点（上机题必背）】
 *
 *   ┌───────────┬───────────────────────────┐
 *   │ #ifdef Z  │ 只看 Z 【有没有】被定义过  │
 *   │ #if Z     │ 看 Z 的【值】是不是非 0    │
 *   └───────────┴───────────────────────────┘
 *
 *   因为 Z 的值是 0，所以：
 *       #ifdef Z 成立（定义过了）
 *       #if Z    不成立（值是 0 = 假）
 *
 *   口诀：#ifdef 里的 d 是 defined（已定义），所以它只问"定义了吗"，
 *         完全不关心值是多少。
 *
 * 【⚠️ 一个延伸的坑】
 *   如果把 Y 写成 `#define Y`（没有值），那 `#if Y` 会展开成 `#if`，
 *   这是【语法错误】，直接编译失败。
 *   所以 `#if` 要求宏必须有个值，而 `#ifdef` 不要求。
 *
 * 【你要做的】
 *   把上面那段程序抄到下面的文件里，运行看看是不是输出 A、D、E。
 *
 * 【期望输出】
 *   A
 *   D
 *   E
 */

#include <stdio.h>

/* TODO 1: 定义 Z 为 0，Y 为 1 */
/* #define Z ... */
/* #define Y ... */

int main(void)
{
    // TODO 2: 写五个条件编译块

    return 0;
}
