/*
 * L07_3 · 哥德巴赫猜想验证
 *
 * 【题目】（经典题）
 *   哥德巴赫猜想：任一大于 2 的偶数都可以写成两个素数之和。
 *   请验证 [6, 50] 范围内的所有偶数，并把每个偶数分解成两个素数之和输出。
 *
 * 【输出格式】
 *   每个偶数一行，格式 n=p+q，其中 p <= q，且 p 取【最小的可能值】。
 *   从 6 开始，每次加 2，一直到 50。
 *
 * 【期望输出的前几行】
 *   6=3+3
 *   8=3+5
 *   10=3+7
 *   12=5+7
 *   14=3+11
 *   ...
 *   50=3+47
 *   （共 23 行）
 *
 * 【思路：外层遍历偶数，内层遍历可能的 p】
 *
 *       int is_prime(int n) { ... }        (素数判断函数，和第 2 题一样)
 *
 *       for (int n = 6; n <= 50; n = n + 2) {
 *           for (int p = 2; p <= n / 2; p++) {
 *               if (is_prime(p) && is_prime(n - p)) {
 *                   printf("%d=%d+%d\n", n, p, n - p);
 *                   break;              (找到第一组就够了，跳到下一个偶数)
 *               }
 *           }
 *       }
 *
 * 【⚠️ 三个关键点】
 *
 *   1. p 只试到 n/2。
 *      因为要求 p <= q（即 p <= n-p），所以 p 最大到 n/2 就够了。
 *      如果试到 n-1，会找到重复的组合（比如 10=3+7 和 10=7+3）。
 *
 *   2. 找到一组后要 break。
 *      题目要求 p 取"最小的可能值"，也就是第一组满足条件的。
 *      不 break 的话会把所有组合都输出，不符合格式要求。
 *
 *   3. 为什么从 6 开始而不是 4？
 *      因为题目要求 [6, 50]。虽然 4=2+2 也成立，但不在范围内。
 *      注意题目说的"范围"就是输出的范围，不要自己扩大。
 *
 * 【为什么这题值得做】
 *   它展示了【用函数让代码变干净】的价值。
 *   如果不写 is_prime 函数，素数判断的代码要塞进内层循环，
 *   又长又乱。抽成函数之后，主逻辑就是三行，一眼看懂在干什么。
 *   🔸 这就是函数存在的意义。
 *
 * 【复杂度提示】
 *   这题的内层循环最多试到 25，外层 23 个偶数，
 *   每次素数判断最多试到 √25 = 5。总计算量非常小，不用优化。
 */

#include <stdio.h>

/* 判断素数：是返回 1，不是返回 0 */
int is_prime(int n)
{
    // TODO
    return 0;
}

int main(void)
{
    // TODO: 遍历 6 到 50 的偶数，每个都分解成两个素数之和

    return 0;
}
