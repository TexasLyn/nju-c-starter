/*
 * L08_1 · 宏展开阅读题
 *
 * 【题目】（经典题）
 *   下面的程序用了几个宏。请【先自己在纸上算】，把结果填进答案里，
 *   然后再写代码运行验证。
 *
 * 【要分析的代码】
 *   ────────────────────────────────────────
 *   #include <stdio.h>
 *   #include <math.h>
 *
 *   #define M 3
 *   #define N M + 1
 *   #define SQR(x) ((x)*(x))
 *
 *   int main(void)
 *   {
 *       printf("%d\n", M * N);
 *       printf("%d\n", SQR(M + 1));
 *       printf("%d\n", SQR(N));
 *       return 0;
 *   }
 *   ────────────────────────────────────────
 *
 * 【算完之后对答案】（先别往下看，自己认真算一遍）
 *
 *   第 1 行 M * N：
 *       M * N  →  3 * N          （M 换成 3）
 *              →  3 * M + 1      （N 换成 M + 1）
 *              →  3 * 3 + 1      （M 再换成 3）
 *              →  10             （先乘后加）
 *       输出 10，不是 12！
 *
 *   第 2 行 SQR(M + 1)：
 *       SQR(x) 是 ((x)*(x))，参数 x 就是 M + 1 这一串
 *       SQR(M + 1)  →  ((M + 1)*(M + 1))
 *                   →  ((3 + 1)*(3 + 1))
 *                   →  16
 *       输出 16。✓ 这次括号救了你。
 *
 *   第 3 行 SQR(N)：
 *       SQR(N)  →  ((N)*(N))
 *               →  ((M + 1)*(M + 1))
 *               →  ((3 + 1)*(3 + 1))
 *               →  16
 *       输出 16。
 *
 * 【本题练习目的】
 *   记住宏的三条规则：
 *     1. 纯文本替换，不是函数调用
 *     2. 替换后会反复进行，直到没有宏名可换
 *     3. 定义带参数的宏时，参数和整体都要加括号，否则必出事
 *
 * 【你要做的】
 *   把上面那段程序写在下面的 main 里（照抄即可），加三句 printf 输出三个结果。
 *
 * 【期望输出】
 *   10
 *   16
 *   16
 */

#include <stdio.h>
#include <math.h>

/* TODO 1: 把三个宏的定义写在下面 */
/* #define M ... */
/* #define N ... */
/* #define SQR(x) ... */

int main(void)
{
    // TODO 2: 三句 printf

    return 0;
}
