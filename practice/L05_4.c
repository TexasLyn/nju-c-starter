/*
 * L05_4 · 六位回文完全平方数
 *
 * 【题目】（经典题）
 *   找一个六位数，它同时满足两个条件：
 *       1. 是回文数（正着读和倒着读一样，比如 abccba）
 *       2. 是完全平方数（是某个整数的平方）
 *   输出这个数和它的平方根。
 *
 * 【输出格式】
 *   两个数，用空格分隔，末尾换行：
 *   698896 836
 *   （因为 836 × 836 = 698896，而 698896 反过来读还是 698896）
 *
 * 【思路一：遍历六位数，筛出回文，再看是不是平方（比较慢）】
 *
 *       for (long n = 100000; n <= 999999; n++) {
 *           if (是回文(n) && 是平方(n)) 输出;
 *       }
 *
 *   判断回文：逐位比较
 *       第1位 == 第6位   →  n / 100000 == n % 10
 *       第2位 == 第5位   →  n / 10000 % 10 == n / 10 % 10
 *       第3位 == 第4位   →  n / 1000 % 10 == n / 100 % 10
 *
 *   判断平方：
 *       for (long r = 317; r <= 999; r++) {
 *           if (r * r == n) { 找到了; break; }
 *       }
 *   （317 是因为 √100000 ≈ 316.2，999 是因为 √999999 ≈ 999.99）
 *
 * 【思路二：反过来遍历平方根（更快，推荐）】
 *   六位数 n = r²，而 r 在 317 到 999 之间。直接遍历 r 只有 683 次：
 *
 *       for (long r = 317; r <= 999; r++) {
 *           long n = r * r;
 *           if (是回文(n)) {
 *               printf("%ld %ld\n", n, r);
 *           }
 *       }
 *
 *   这个思路更好，因为候选数从 90 万个减少到 683 个。
 *   🔸 这体现了一个重要技巧：【穷举的时候，选择"更少的那一边"来遍历。】
 *   上机题里遇到穷举题，先想一想有没有办法把搜索范围缩小。
 *
 * 【结果唯一吗】
 *   六位回文完全平方数只有一个，就是 698896。
 *   但用思路二写的话，每个回文数都会输出，所以只会输出一行。
 *
 * 【⚠️ 用 long 更安全】
 *   r * r 最大到 998001，int 装得下，但用 long 是习惯。
 *   注意 long 的格式符是 %ld。
 */

#include <stdio.h>

int main(void)
{
    // TODO: 写你的穷举

    return 0;
}
