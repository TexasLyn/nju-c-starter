/*
 * L03_4 · 同一个数，不同精度输出
 *
 * 【题目】（经典题）
 *   读入一个浮点数，把它分别四舍五入到：
 *       十分位（保留 1 位小数）
 *       百分位（保留 2 位小数）
 *       千分位（保留 3 位小数）
 *       万分位（保留 4 位小数）
 *   每行输出一个。
 *
 * 【输入格式】
 *   一个浮点数，例如 100.453627
 *
 * 【输出格式】
 *   四行，分别保留 1、2、3、4 位小数：
 *
 *   100.5
 *   100.45
 *   100.454
 *   100.4536
 *
 * 【提示】
 *   这道题就是考 %.Nf 这个写法。四行 printf 搞定：
 *       printf("%.1f\n", x);
 *       printf("%.2f\n", x);
 *       printf("%.3f\n", x);
 *       printf("%.4f\n", x);
 *
 * 【⚠️ 一个值得知道的坑】
 *   printf 的 %.Nf 会**四舍五入**，但因为是二进制浮点数，
 *   某些情况下会"舍错"。比如经典例子：
 *       printf("%.2f\n", 2.675);     // 可能输出 2.67，不是 2.68
 *   原因是 2.675 在二进制里存的其实是 2.67499999...，所以它确实该舍。
 *   这不是 bug，是浮点数的本质。上机题遇到这种题按 "%.Nf 就是四舍五入" 答即可。
 */

#include <stdio.h>

int main(void)
{
    double x;

    scanf("%lf", &x);

    // TODO: 四行 printf，分别保留 1、2、3、4 位小数

    return 0;
}
