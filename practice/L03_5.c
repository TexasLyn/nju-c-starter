/*
 * L03_5 · 9 位整数拆成三个三位数
 *
 * 【题目】（经典题）
 *   读入一个 9 位正整数，把它从高位到低位平均拆成三个三位数，输出。
 *
 * 【输入格式】
 *   一个 9 位数，例如 123456789
 *
 * 【输出格式】
 *   三个三位数，用空格分隔，末尾换行：
 *   123 456 789
 *
 * 【⚠️ 为什么必须用 long】
 *   9 位数最大是 999999999，约 10 亿。
 *   int 的范围是 ±21 亿 —— 装是装得下，但很勉强。
 *   上机题里遇到"很大很大的整数"，直接用 long 更安全。
 *
 *   long 的读写格式符是 %ld（注意那个 l）：
 *       long n;
 *       scanf("%ld", &n);
 *       printf("%ld\n", n);
 *
 * 【提示】
 *   用除法和取余，从高位切：
 *
 *       123456789 / 1000000      = 123       ← 高三位
 *       123456789 / 1000 % 1000  = 456       ← 中三位
 *       123456789 % 1000         = 789       ← 低三位
 *
 *   中间那个"除以 1000 再 % 1000"的意思：
 *       先 / 1000 把低三位砍掉，得到 123456
 *       再 % 1000 取低三位，得到 456
 */

#include <stdio.h>

int main(void)
{
    long n;

    scanf("%ld", &n);

    // TODO: 用 / 和 % 把 n 拆成三个三位数，打印出来

    return 0;
}
